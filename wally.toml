[package]
name = "vynx777/networker"
description = "Namespace-based networking layer for Roblox. Routes all communication through a single RemoteEvent and RemoteFunction per namespace via dispatch tables, eliminating stacked connections."
version = "0.0.4"
registry = "https://github.com/UpliftGames/wally-index"
realm = "shared"
authors = ["vynx777"]
license = "MIT"

[dependencies]